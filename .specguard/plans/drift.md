---
pipeline: drift
generatedAt: 2026-06-29T02:38:31.080Z
status: pending
---

# Fix Spec Drift — 1 spec(s) out of sync

> The drift pipeline found 1 Living Spec(s) that no longer match their source code. Update each spec to reflect the current implementation, then re-run drift to verify.

## Drifted Specs

- `specguard-pipelines/gap-analysis` — semantic drift in src/pipelines/gap-analysis.ts: The diff introduces a new summary plan file written via `writePlan` for the coding agent whenever gaps are found, which is a new output artifact not described anywhere in the spec (the spec only mentions per-spec plans under `.specguard/plans/<feature>.md` for unimplemented specs and `.specguard/gaps.json`).

## Fix Steps

1. Open each drifted spec file listed above.
2. Review the acceptance criteria and scenarios against the current source code.
3. Update the spec to reflect the actual implementation.
4. If the spec describes intended behaviour that is now missing from the source, restore the source code instead.
5. Run `specguard drift` again to confirm no remaining drift.

---

_Generated by SpecGuard `drift` pipeline · 2026-06-29T02:38:31.080Z_
_Pass this file to your coding agent to apply the fixes automatically._