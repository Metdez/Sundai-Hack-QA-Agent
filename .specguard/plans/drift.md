---
pipeline: drift
generatedAt: 2026-06-29T04:09:21.640Z
status: pending
---

# Fix Spec Drift — 2 spec(s) out of sync

> The drift pipeline found 2 Living Spec(s) that no longer match their source code. Update each spec to reflect the current implementation, then re-run drift to verify.

## Drifted Specs

- `specguard-pipelines/analyze` — semantic drift in src/pipelines/analyze.ts: AC2 mandates that the `reverse` recommendation always carries `high` priority when `MissingSpecs` is returned, but the diff introduces coverage-percentage-based priority logic that can produce `medium` or `low` priority instead.
- `specguard-pipelines/doc-generate` — semantic drift in src/pipelines/doc-generate.ts: The diff introduces a new post-processing step that calls `syncRootDocs` to update README.md, CLAUDE.md, and AGENTS.md after per-spec doc generation, which is not described anywhere in the spec's acceptance criteria.

## Fix Steps

1. Open each drifted spec file listed above.
2. Review the acceptance criteria and scenarios against the current source code.
3. Update the spec to reflect the actual implementation.
4. If the spec describes intended behaviour that is now missing from the source, restore the source code instead.
5. Run `specguard drift` again to confirm no remaining drift.

---

_Generated by SpecGuard `drift` pipeline · 2026-06-29T04:09:21.640Z_
_Pass this file to your coding agent to apply the fixes automatically._